events {}
http {
  access_log off;
  upstream board_pool {
    least_conn;
    server board-bench-a:8091;
    server board-bench-b:8091;
  }
  server {
    listen 8080;
    location / { proxy_pass http://board_pool; }
  }
}
