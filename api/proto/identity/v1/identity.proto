// Placeholder — the Identity service contract is defined in Phase 1
// (OTP request/verify, token issuance/refresh, signup completion).
syntax = "proto3";

package identity.v1;

option go_package = "identity-service/api/gen/identity/v1;identityv1";
